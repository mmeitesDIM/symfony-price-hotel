{% extends 'base.html.twig' %}

{% block title %}PrivateRoom{% endblock %}

{% block body %}
    <h1>PrivateRoom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ private_room.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ private_room.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ private_room.address }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ private_room.postalCode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ private_room.city }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ private_room.country }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ private_room.surface }}</td>
            </tr>
            <tr>
                <th>BedNumber</th>
                <td>{{ private_room.bedNumber }}</td>
            </tr>
            <tr>
                <th>AvgRated</th>
                <td>{{ private_room.avgRated }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('private_room_index') }}">back to list</a>

    <a href="{{ path('private_room_edit', {'id': private_room.id}) }}">edit</a>

    {{ include('private_room/_delete_form.html.twig') }}
{% endblock %}
