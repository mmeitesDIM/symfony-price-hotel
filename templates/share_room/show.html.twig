{% extends 'base.html.twig' %}

{% block title %}ShareRoom{% endblock %}

{% block body %}
    <h1>ShareRoom</h1>

    <div>
        <p>coucou</p>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ share_room.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ share_room.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ share_room.address }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ share_room.postalCode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ share_room.city }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ share_room.country }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ share_room.surface }}</td>
            </tr>
            <tr>
                <th>BedNumber</th>
                <td>{{ share_room.bedNumber }}</td>
            </tr>
            <tr>
                <th>AvgRated</th>
                <td>{{ share_room.avgRated }}</td>
            </tr>
            <tr>
                <th>PlaceLeaving</th>
                <td>{{ share_room.placeLeaving }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('share_room_index') }}">back to list</a>

    <a href="{{ path('share_room_edit', {'id': share_room.id}) }}">edit</a>

    {{ include('share_room/_delete_form.html.twig') }}
{% endblock %}
